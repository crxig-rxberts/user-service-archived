spring:
  datasource:
    password: password
    url: jdbc:postgresql://user-service.cd2hwitlzmny.eu-west-1.rds.amazonaws.com:5432/user-service
    username: ubuntu
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true